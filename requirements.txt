# ============================================================
# Christian Sermon Counseling Chatbot - Base Dependencies
# ============================================================
# Python: 3.11 (확인됨: 3.11.7)
#
# 이 파일은 Streamlit 로컬 UI + FastAPI 백엔드 + RAG 검색에 필요한
# 최소 의존성만 포함합니다. Render 배포에도 이 파일을 그대로 사용.
#
# 임베딩 생성 파이프라인(whisper 전사, yt-dlp 다운로드)을 돌리려면
# 추가로 `requirements-pipeline.txt` 를 설치하세요:
#   pip install -r requirements-pipeline.txt
# ============================================================

# --- 핵심 (웹 UI / 환경 설정) ---
streamlit==1.44.0
python-dotenv==1.0.1

# --- 백엔드 API (Render 배포용) ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- AI / 검색 ---
openai==1.61.1
faiss-cpu==1.10.0

# --- 데이터 처리 ---
numpy==2.1.3
pandas==2.2.3
openpyxl==3.1.5
